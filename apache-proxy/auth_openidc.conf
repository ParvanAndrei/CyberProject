# Define the OIDC provider metadata endpoint URL
OIDCProviderMetadataURL https://accounts.example.com/.well-known/openid-configuration

# Define the client ID and secret obtained when registering your web application with the OIDC provider
OIDCClientID CyberProjectClient
OIDCClientSecret TxrOKhT40unLypYNa9g5eAEVuX88Q1VL

# Define the redirect URI where the OIDC provider will redirect users after authentication
OIDCRedirectURI http://localhost:80/energy/redirect_uri

# Define the URI where the OIDC provider will redirect users if authentication fails
# OIDCRedirectErrorURI https://www.example.com/auth/oidc/error

# Define the URL to retrieve the public key for token validation
# OIDCJWKSUri https://accounts.example.com/.well-known/jwks.json

# Define the scope of the OIDC authentication request
OIDCScope "openid email profile"

# Define the URL to validate the user's session state
# OIDCSessionType client

# Define the SSL certificate authority file
# OIDCCACertFile /etc/ssl/certs/ca-certificates.crt

# Define the SSL certificate authority directory
# OIDCCACertPath /etc/ssl/certs/

# Define the SSL certificate authority files for token validation
# OIDCSSLValidateServer Off

# Define additional OIDC configuration options as needed
# ...

# Define the location of the user session storage
# OIDCSessionCacheSize 1000
# OIDCSessionCacheDBFile /var/run/mod_auth_openidc_session.dbm
